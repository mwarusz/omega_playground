foreach(omega_lib IN LISTS OMEGA_LIBS)
  target_sources(${omega_lib} PRIVATE mpas_mesh.cpp planar_hexagonal_mesh.cpp)
  if (${netCDFCxx_FOUND})
    target_sources(${omega_lib} PRIVATE file_mesh.cpp)
  endif()
endforeach()
